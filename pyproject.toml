[build-system]
requires = ["setuptools >= 77.0.3"]
build-backend = "setuptools.build_meta"

[project]
name = "january_ml"
version = "0.0.1"
description = "Internal utilities for January ML team"
readme = "january_ml/README.md"
requires-python = ">=3.9"

[tool.setuptools]
packages = ["january_ml"]